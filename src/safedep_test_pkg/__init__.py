__all__ = ["hello_world"]

# __version__ is generated by setuptools_scm at build time into _version.py
try:
    from ._version import version as __version__  # type: ignore
except Exception:
    # Fallback for non-built editable/dev environments without VCS metadata
    __version__ = "0.0.0"


def hello_world() -> str:
    return "Hello, world!"
